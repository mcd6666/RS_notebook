# 遥感影像 Token 计算指南 (Guide to Remote Sensing Token Calculation)

在遥感深度学习中，Token 是 Transformer 模型（如 ViT, DINOv3）处理信息的最小单元。准确计算 Token 数量对于预估显存占用和 API 成本至关重要。

![image](图片/6a70550607f85a08d4f3fa9ce875f72c.png)

---

## 1. 核心计算公式 (Core Formula)

对于任意一幅影像，其产生的空间 Token 总量由其空间分辨率（长宽）和模型设定的块大小（Patch Size）决定：

$$Token_{total} = \lceil \frac{H}{P} \rceil \times \lceil \frac{W}{P} \rceil$$

- **H (Height)**: 影像高度（像素个数）
- **W (Width)**: 影像宽度（像素个数）
- **P (Patch Size)**: 图像块边长（常见为 14, 16, 32）
- **$\lceil \cdot \rceil$**: 向上取整符号，表示边缘不足一块时进行补齐 (Padding)。

---

## 2. 关键参数解析

### 2.1 切片大小 (Slice/Tile Size)
为了适配显存，大面积遥感影像（如 40000x40000）通常被切割成较小的单位进行训练/推理。
- **推荐值**: 256, 512, 1024。
- **作用**: 它是模型单次处理的“视野”范围。

### 2.2 图像块大小 (Patch Size)
它是模型内部注意力的最小单元。
- **16x16 ($P=16$)**: 工业界最均衡的基准。
- **14x14 ($P=14$)**: DINOv2/v3 常用，比 16 稍微精细，显存占用稍高。
- **8x8 或更小**: 用于极致的细粒度分割（如潮沟、米草边缘），但计算量呈平方级爆炸。

### 2.3 批大小 (Batch Size)
- **定义**: 一次并行送入显卡训练的切片个数。
- **公式**: $Total\_Tokens\_Per\_Batch = Batch\_Size \times Token_{total}$。

---

## 3. 常见误区与避坑指南

### ❌ 误区一：波段数（Bands）会增加 Token 数量
**事实**: 波段数（如 RGB, 近红外, 短波红外）只增加每个 Patch 的特征深度，**不改变 Token 总数**。无论 3 波段还是 10 波段，Token 数量一致。

### ❌ 误区二：衍生产物（指数）会增加 Token 数量
**事实**: 叠加 NDVI、NDWI 或多时相影像，在模型底层通常被视为增加输入通道（Channels）。它们被 Linear Projection 层压缩成 1 个 Token，不增加序列长度。

### ❌ 误区三：Token 数量 = 像素个数
**事实**: 1 个 Token 通常包含 $P^2$ 个像素。例如 $P=16$ 时，1 个 Token 代表 256 个像素的信息集成。

---

## 4. 商业 API 计费逻辑 (Commercial Logic)

商业模型（如 GPT-4v, Doubao-Vision）通常采用 **32x32** 的网格进行计费，并设有“最大预算”：

1. **初步切块**: $P_{32} = \lceil W/32 \rceil \times \lceil H/32 \rceil$。
2. **预算限制**: 如果 $P_{32}$ 超过预设上限（如 1536），影像会被等比例**强制缩放**至符合预算为止。
3. **最终计费**: $Billed\_Tokens = Resized\_Patches \times Model\_Multiplier$。

---

## 5. 快速换算表 (以 512x512 切片为例)

| Patch Size | Token 数量 | 像素/Token 压缩比 | 适用场景 |
| :--- | :--- | :--- | :--- |
| **32** | 256 | 1024:1 | 粗略分类、快速筛选 |
| **16** | 1024 | 256:1 | 标准遥感目标检测与分割 |
| **14** | 1333 | 196:1 | 基础模型预训练、通用特征提取 |
| **8** | 4096 | 64:1 | 极细碎生境要素提取（如米草斑块） |

---
*Generated by Gemini for Remote Sensing Research.*
